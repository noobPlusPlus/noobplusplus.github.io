---
layout: post
title: C和指针初窥
category : 技术
tagline: "Supporting tagline"
tags : [C, C和指针]
published: true
---
{% include JB/setup %}
# C和指针初窥

---

介于最近马上要找工作啦，之前想从头开始写的Python教程，看起来是又要落空了，好吧！那我就从头写个《C和指针》的练习吧！～

## Chapter 1 快速上手
书上第一章，首先给出了一道字符串的题。但是代码其实是有问题的，我使用的是 2008年4月第2版,2010年11月北京第12次印刷。

<!--break-->

先看代码：

主函数：

```C
/*
 * 这个程序从标准输入中读取输入行并在标准输出中打印这些输入行，
 * 每个输入行的后面一行是该行内容的一部分。
 *
 * 输入的第1行是一串列标号，串的最后以一个负数结尾。
 * 这些列标号成对出现，说明需要打印的输入行的列的范围。
 * 例如，0 3 10 12 -1 表示第0列到第3列，第10列到第12列的内容将被打印。
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#define MAX_COLS 20	/* 所能处理的最大列号 */
#define MAX_INPUT 1000	/* 每个输入行的最大长度 */

int read_column_numbers(int columns[], int max);
void rearrange(char *output, char const *input, int n_columns, int const columns[]);

int main(void)
{
	int n_columns;	// 进行处理的列标号
	int columns[MAX_COLS];	// 需要处理的列数
	char input[MAX_INPUT];	// 容纳输入行的数组
	char output[MAX_INPUT];	// 容纳输出行的数组

	/*
	 * 读取该串列标号
	 */
	n_columns = read_column_numbers(columns, MAX_COLS);

	/*
	 * 读取、处理、打印剩余的输入行
	 */
	while(fgets(input, MAX_INPUT, stdin) != NULL)
	{
		printf("Original input: %s\n", input);
		rearrange(output, input, n_columns, columns);
		printf("Rearrange line: %s\n", output);
	}
	return EXIT_SUCCESS;
	}
```

read_column_numbers 函数

```C
/*
 * 读取列标号，如果超出规定范围则不予例会。
 */
int read_column_numbers(int columns[], int max)
{
	int num = 0;
	int ch;

	/*
	 * 获取列标号，如果所读取的数小于0则停止。
	 */
	while(num < max && scanf("%d", &columns[num])==1
			&& columns[num] >= 0)
		num += 1;

	/*
	 * 确认已经读取的标号为偶数个，因为他们是以对的形式出现的。
	 */
	if(num%2!=0)
	{
		puts("Last column number is not paired.");
		exit(EXIT_FAILURE);
	}

	/*
	 * 丢弃该行中包含最后一个数字的那部分内容。
	 */
	while((ch=getchar())!=EOF && ch!='\n')
		;

	return num;
}
```

rearrange 函数

```C
/*
 * 处理输入行，将制定列的字符链接在一起，输出行以NUL结尾。
 */
void rearrange(char *output, char const *input,
		int n_columns, int const columns[])
{
	int col;	// columns数组的下标
	int output_col;	//输出列计数器
	int len;	// 输入行的长度

	len = strlen(input);
	output_col = 0;

	/*
	 * 处理每对列标号。
	 */
	for(col = 0; col < n_columns; col += 2)
	{
		int nchars = columns[col+1] - columns[col] + 1;

		/*
		 * 如果输入行结束或输出行数组已满，就结束任务。
		 */
		if(columns[col] >= len || output_col == MAX_INPUT -1)
			break;

		/*
		 * 如果输出行数据空间不够，只复制可以容纳的数据。
		 */
		if(output_col + nchars > MAX_INPUT - 1)
			nchars = MAX_INPUT - output_col - 1;

		/*
		 * 复制相关的数据。
		 */
		strncpy(output + output_col, input + columns[col], nchars);
		output_col += nchars;
	}
}
```

### 各种情况
这是一个很简单的字符串题，根据第一行要求的数字，对每一行的字符串提取部分，拼接起来，并输出。

1, 是代码中已经考虑到的，第一行输入不是成对出现的。
2, 输出行空间不够，书中也考虑到了。

这样子看似运行能够这正常，事实上，来试试。

```
./a.out
3 5 7 9 -1
abcdefghijklmnopqrstuvwxyz
Original input: abcdefghijklmnopqrstuvwxyz
Rearrange line: defhij
ab
Original input: ab
Rearrange line: defhij
```

可以看出，这里的问题是，当输入小于第一个列数时，数组没有被做清空处理。只需要在`main()`的`while()`循环中写入，`output[0] = '\0'`即可。

当然，我们也可以来玩一玩。
试想，如果把数组的声明放到`while()`循环中，会发生什么？按照我的理解，是数组使用完就被清空了，再次使用，会申请新的数组。试试。

`main()`函数代码改写：

```C
while(fgets(input, MAX_INPUT, stdin) != NULL)
{
	char output[MAX_INPUT];
	printf("Original input: %s\n", input);
	rearrange(output, input, n_columns, columns);
	printf("Rearrange line: %s\n", output);
}
```

测试结果：

```
3 7 9 10 -1
alsdjflajsdglajsdlgjasld
Original input: alsdjflajsdglajsdlgjasld
Rearrange line: djflasd
af
Original input: af
Rearrange line: djflasd
a
Original input: a
Rearrange line: djflasd
s
```

依然是错误的。按理说，`while()`循环内部的变量使用完立即释放，但是新申请的内存却依旧存在。
如何测试呢？

```C
int index = 0;
while(fgets(input, MAX_INPUT, stdin) != NULL)
{   
	if(index % 2 == 0)
	{
		char newout[MAX_INPUT]; // 新建一个数组 
		printf("newout address: 0x%x\n", &newout);
	}
	char output[MAX_INPUT]; // 容纳输出行的数组
	printf("output address: 0x%x\n", &output);
	printf("Original input: %s\n", input);
	rearrange(output, input, n_columns, columns);
	printf("Rearrange line: %s\n", output);
	index ++; 
}   
```

得到什么样的结果呢？

```
3 5 7 9 -1
abcdefghijklmn
newout address: 0xe4a0e610
output address: 0xe4a0e610
Original input: abcdefghijklmn
Rearrange line: defhij
abcdefghijklmn
output address: 0xe4a0e610
Original input: abcdefghijklmn
Rearrange line: defhij
abcdefghijklmn
newout address: 0xe4a0e610
output address: 0xe4a0e610
Original input: abcdefghijklmn
Rearrange line: defhij
```

看上去是个很奇怪的结果啊，似乎`while()`循环从来没有释放过数组，但是仔细想想，如果`while()`每次结束后，没有释放数组的空间，`if`语句中的`newout`数组是如何申请到一样的空间的呢？
这样的结果说明了两点，1. `while`和`if`内部的变量都会在每次循环中被释放。 2.`while`太快，导致每次都被分配到同一块地址空间。

8月15完成。

{% if page.comments %}
<div id="disqus_thread"></div>
<script>
	(function() {  // DON'T EDIT BELOW THIS LINE
	var d = document, s = d.createElement('script');
	s.src = '//nzyt1githubio.disqus.com/embed.js';
	s.setAttribute('data-timestamp', +new Date());
	(d.head || d.body).appendChild(s);
})();
</script>
<noscript>Please enable JavaScript to view the <a href="https://disqus.com/?ref_noscript" rel="nofollow">comments powered by Disqus.</a></noscript>
{% endif %}
